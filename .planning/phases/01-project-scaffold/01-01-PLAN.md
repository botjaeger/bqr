---
phase: 01-project-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - components/ui/button.tsx
  - .eslintrc.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `npm run dev` serves the app at localhost with no TypeScript errors"
    - "Tailwind v4 styles are applied — at least one className utility is visibly rendered"
    - "A shadcn/ui Button component renders on the page without error"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies including next@15.5, tailwindcss@4, and shadcn/ui"
    - path: "tsconfig.json"
      provides: "TypeScript strict mode config"
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS-first import (@import \"tailwindcss\")"
    - path: "app/layout.tsx"
      provides: "Root layout with Tailwind class applied"
    - path: "app/page.tsx"
      provides: "Index page rendering a shadcn/ui Button"
    - path: "components/ui/button.tsx"
      provides: "shadcn/ui Button primitive (copied by CLI)"
  key_links:
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "components/ui/button.tsx"
      to: "app/page.tsx"
      via: "named import"
      pattern: "import.*Button.*from.*components/ui/button"
---

## Phase Goal

**As a** developer, **I want to** bootstrap a Next.js 15 project with TypeScript, Tailwind v4, and shadcn/ui, **so that** every subsequent phase has a consistent, type-safe foundation to build on.

<objective>
Scaffold a new Next.js 15.5 project with TypeScript strict mode, Tailwind CSS v4 (CSS-first, no tailwind.config.js), and shadcn/ui. The deliverable is a locally-running dev server with a page that renders a shadcn/ui Button — proving the entire styling and component pipeline is wired end-to-end.

Purpose: Establish the architectural foundation that all ten phases build on.
Output: A runnable Next.js app with Tailwind v4 styles and one shadcn/ui component visible in the browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with TypeScript strict mode and Tailwind v4</name>
  <files>package.json, tsconfig.json, next.config.ts, app/globals.css, app/layout.tsx, app/page.tsx, .eslintrc.json</files>
  <read_first>
    - Read this PLAN.md fully before touching any file.
    - No existing source files — greenfield project at /Users/jarngotostos/zzz/bqr.
  </read_first>
  <action>
    Initialize the project with `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir no --import-alias "@/*"` inside /Users/jarngotostos/zzz/bqr. Accept all defaults.

    After scaffolding, perform these four mutations:

    1. tsconfig.json — verify "strict": true is present under "compilerOptions". If not, add it. Also verify "noEmit": true and "moduleResolution": "bundler" are present.

    2. app/globals.css — replace the entire file content with exactly two lines:
       `@import "tailwindcss";`
       followed by any custom CSS variables you want to keep. Remove all `@tailwind base/components/utilities` directives — they do not exist in Tailwind v4. The single @import is the entire Tailwind v4 entry point.

    3. app/layout.tsx — ensure the root layout imports ./globals.css and applies className="min-h-screen bg-background text-foreground" on the <html> element (or equivalent Tailwind utilities). The layout must include {children} in a <body> tag.

    4. app/page.tsx — replace generated content with a minimal page: import and render a placeholder `<main className="flex min-h-screen items-center justify-center"><p className="text-2xl font-bold">BQR</p></main>`. This is temporary — Task 2 adds the Button component.

    Install Tailwind v4 correctly: create-next-app with --tailwind installs tailwindcss@4 by default in 2025. Verify package.json shows "tailwindcss": "^4.x". If it shows ^3.x, run `npm install tailwindcss@latest` to upgrade.

    Do NOT create a tailwind.config.js or tailwind.config.ts — Tailwind v4 is CSS-first; the config file is replaced by @theme directives in globals.css.
  </action>
  <verify>
    <automated>cd /Users/jarngotostos/zzz/bqr && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes with no TypeScript errors and no Tailwind errors.
    - package.json contains "tailwindcss": with a version starting "4." (not "3.").
    - No tailwind.config.js or tailwind.config.ts file exists.
    - app/globals.css contains `@import "tailwindcss"` and does NOT contain `@tailwind base` or `@tailwind components`.
    - tsconfig.json "compilerOptions" contains `"strict": true`.
  </acceptance_criteria>
  <done>Next.js 15 builds cleanly with TypeScript strict and Tailwind v4 CSS-first import.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn/ui and render Button on the index page</name>
  <files>components/ui/button.tsx, app/page.tsx, components.json</files>
  <read_first>
    - Read app/page.tsx (created in Task 1) before editing.
    - Read app/globals.css to confirm Tailwind v4 import is present before shadcn init.
  </read_first>
  <action>
    Run `npx shadcn@latest init` inside /Users/jarngotostos/zzz/bqr. When prompted:
    - Style: Default (or New York — either is acceptable)
    - Base color: Slate
    - CSS variables: Yes
    - Tailwind config: shadcn will detect Tailwind v4 and skip generating tailwind.config.js — confirm yes if asked.
    - aliases: accept defaults (@/components, @/lib/utils)

    After init, add the Button component: `npx shadcn@latest add button`

    This copies components/ui/button.tsx and lib/utils.ts into the project.

    Then update app/page.tsx: import Button from "@/components/ui/button" and render it inside the existing <main> element. The Button text must be "Generate QR" — this is the primary CTA for the app and establishes the page's purpose from the first scaffold. The full page content:

    A centered layout with:
    - "BQR" heading (text-4xl font-bold)
    - "Mosaic QR Code Generator" subtitle (text-muted-foreground)
    - A `<Button>Generate QR</Button>` (non-functional at this stage — wired to Canvas in Phase 2)

    Do NOT add state, hooks, or interactivity — this is a static render to confirm the component pipeline.
  </action>
  <verify>
    <automated>cd /Users/jarngotostos/zzz/bqr && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with zero TypeScript errors.
    - components/ui/button.tsx exists and exports a Button component.
    - lib/utils.ts exists and exports a `cn` function.
    - app/page.tsx imports Button from "@/components/ui/button".
    - components.json exists (shadcn/ui project manifest).
    - `grep -r "Generate QR" app/page.tsx` returns a match.
  </acceptance_criteria>
  <done>shadcn/ui Button renders on the index page and npm run build passes with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm install | Third-party packages installed from npm registry into dev environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm package installs (create-next-app, shadcn CLI) | accept | All packages are Microsoft/Vercel/shadcn official CLIs with millions of weekly downloads; package legitimacy is high confidence |
| T-01-02 | Information Disclosure | TypeScript strict mode off | mitigate | Enforce "strict": true in tsconfig.json per Task 1 — prevents silent type coercions that could hide security issues in later phases |
| T-01-SC | Tampering | npm/npx installs | mitigate | All packages used (next, tailwindcss, shadcn) are tier-1 ecosystem packages; no unknown third-party packages introduced in this plan |
</threat_model>

<verification>
After both tasks complete, confirm:
1. `npm run build` exits 0 with no TypeScript or Tailwind errors.
2. `npm run dev` starts the dev server (no crash).
3. `grep -c '"strict": true' tsconfig.json` returns 1.
4. `grep -c '@import "tailwindcss"' app/globals.css` returns 1.
5. File exists: components/ui/button.tsx.
6. File exists: components.json.

<verify>
  <human-check>
    Start `npm run dev` and open http://localhost:3000 in a browser. Confirm:
    - Page loads without console errors.
    - "BQR" heading is visible and styled (font-bold applied).
    - "Generate QR" button is visible and appears with shadcn/ui styling (not unstyled HTML button).
    - Background color reflects the Tailwind/shadcn CSS variables (not plain white with default browser styles).
  </human-check>
</verify>
</verification>

<success_criteria>
- `npm run build` passes with zero errors.
- Tailwind v4 is confirmed (package.json version ^4.x, no tailwind.config.js, @import in globals.css).
- shadcn/ui Button is rendered on the page.
- TypeScript strict mode is enabled.
</success_criteria>

<output>
Create `.planning/phases/01-project-scaffold/01-01-SUMMARY.md` when done.
</output>
